---
name: update
description: Alias for /wrap, the task finish line. Use when a task is done to run the full wrap sequence: gate, docs, commit, merge, cleanup, push, handoff.
---

**Alias for `/wrap`.** The finish-line moved to one verb. See `wrap.md` — or just say `/wrap`.

Run the steps in `wrap.md`: gate (`/check`) → checked docs write → fold + delete lane → commit → merge to main → remove worktree → push → handoff.
